/*
  1. Add a constructor with a storage structure; there are multiple options you could use for this
  2. Add a size getter that returns the number of items the stack is storing
  3. Add a `push` method that accepts an item as input and adds it to the storage structure
  4. Add a `pop` method that removes the most recently-added item to the stack
*/
class Stack {
// Add a constructor with a storage structure;
  constructor() {
    this.storage = [];
    this.count = 0;
  }

// push a value
  this.push(5);
  this.push(10);
  
// Add a size getter that returns the number of items the stack is storing
get size() {
  return this.count;
}
// Add a `push` method that accepts an item as input and adds it to the storage structure

// Add a `pop` method that removes the most recently-added item to the stack

}

module.exports = Stack;
